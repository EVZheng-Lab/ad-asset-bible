---
name: ad-asset-bible
description: "Lira 的上游资产盘点器——把一份剧本变成一个 HTML 页，三张同构的表：场景 | 角色·服装 | 关键道具，每张都是「资产 | 需求描述 | 变体」。它回答的是资产问题:这个剧本有几处地方、几个人几套衣服、几件跨场景的道具，每份要几个变体，每场戏用哪一个,不漏任何一份。场景按地点切分去重（靠台词证据判断两个地名是不是同一处）；角色拆成「底档（脸/年龄/身形，写一遍）+ 每套服装一份资产 + 状态变体」，因为出图单元是「角色+一套服装」、换整套衣服图生图没有共同像素；道具只收跨场景的关键道具，并登记它画面上间接引用了谁（照片/油画/建筑画 → 必须走参考生图）。用 /ad-asset-bible [剧本路径] 触发,不传路径时从当前对话判断。当用户说'这个剧本有哪些场景/出个场景清单/场景盘点/资产盘点/角色盘点/服装盘点/道具盘点/几个场景几个变体/有哪些角色几套服装/关键道具有哪些/给Lira的场景描述'时使用。★ 描述刻意只有几句话:图像模型对'中式庭院''18岁少年''一枚玉佩'这类东西有极强先验,穷举清单会把先验压掉、出图反而更差(实测过)。所以不写尺寸、不写材料清单、不写机位光线、不写配色方案、不写表情性格,只写这是什么东西、什么调性、哪一处是认得出的参照物。固有色（藏青夹克、褪成灰蓝的门牌）可以写。年代与国别定个大概,不做深度考据。不决定怎么拍(Lira 的活)、不做戏剧结构判断、不生成图像、不改剧本。"
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch
---

# ad-asset-bible（副导演-资产盘点）— 剧本 → 资产盘点（Lira 的上游）

你交付的是一份**资产盘点**：这个剧本有几处地方、几个人几套衣服、几件跨场景的道具，
每份要几个变体，每场戏用哪一个。

产出是一个 HTML 页，**三个 Tab、每个 Tab 一张结构相同的表**，每张四列：**序号 | 资产 | 需求描述 | 变体**。

| Tab | 一句话 |
|---|---|
| **场景** | 这个剧本有几个地方、每场戏在哪一处 |
| **角色 · 服装** | 出图单元是「角色 + 一套服装」；脸只有一份底档 |
| **关键道具** | 只收跨场景的；画面上引用了别人形象的必须走参考生图 |

Tab 栏右端还有一个**「全部展开」**开关（`?tab=all`）——
三张表同时铺开，跨表 Ctrl+F 和无头截图自检都要用它。
角色/道具那两张表为空时对应的 Tab 不出现；只剩一张表时整个 Tab 栏都不出。

**你不生成图像，不调用 Lira，不改剧本。**

---

## ★ 先读这一节：为什么描述只写几句话

这个 skill 走偏过一次，教训值钱，写在最前面。

为了让出图"更真实"，我曾给每个场景写上千字规格 —— 尺度到厘米、材分制、11 件带位置的道具表、
前中后景清单 —— 并为每一项加了闸门。**结果图越来越差。**
而用户用「一个中式庭院」五个字出的图比它们都好。

原因不是措辞不对，是方向不对。三条源码印证：

**1. 图像模型对常见东西有极强先验。** "中式庭院"背后是成千上万张园林照片，
隐含几百样细节。穷举清单等于告诉它"就这些，别的没有"，**把先验压掉了**。

**2. Lira anti-fail #2 早就写着**：
> A tight 80–150-word prompt beats a scattered 400-word one: past a point every extra
> clause dilutes attention and **details drop out**. Cut filler; **keep anchors**.

**3. Soul Cinema 那句说了两遍**：`the model carries them natively — one register line is enough`

**所以：不写尺寸、不写材料清单、不写道具表、不写机位光线。**
只写：这是什么东西、什么身份档次、什么调性、哪一处是认得出的参照物。
`build.mjs` 把每份需求描述卡在 **300 字**以内 —— 那不是偷懒的上限，是**质量上限**。

★ **这条对角色和道具一样成立，而且更容易犯。** 图像模型对"二十七八岁男性方脸"
和"一把老式黄铜钥匙"的先验同样强。穷举五官比例、列齿口尺寸，
跟给场景写"墙下隔减广约 1.09m"是**同一个错**。

> 这不与"我们是上游、不受 Lira 字数纪律约束"矛盾。Lira 的 80–150 词管的是它**产出**的英文提示词；
> 我们的 300 字是另一件事 —— 是"写多了会压掉先验"这条实测结论。**两者恰好同向。**

---

## ★ 这个 skill 为什么存在

Lira 是"懂器材脾气的技术执行者"。它的 DIAGNOSE 缺口清单（`LIRA SKILL.md:40-41`）是：

```
Find gaps in clarity and ambiguity (camera angle, light, palette, subject count, framing)
```

**五项全是"怎么拍"，没有一项是"这是哪儿、这是谁、有几个"。** 所以它**不会补也不会问** ——
会往 `[Key architectural elements]` 槽位填一个统计意义上最平均的场景。那是兜底不是补齐。

而官方把 `environment invention` 列为 **`the #1 drift source`**
（`evals/cases/harvest.json:45`，regex 强制；`CHANGELOG.md:670` 注明 `above character drift`），
解法是一条锁：`The set contains only what the reference shows.`
**锁造好了，填锁的东西没造** —— 这个锁只在"已经有人决定了资产清单"时才成立。那个人是你。

★ 那句 `above character drift` 顺便说明了**第二号漂移源是角色**。
本 skill 对它的解法是**底档**：脸只写一遍，所有服装资产引用它。
每份服装各写一遍脸，三遍之间必然对不齐 —— 那就是漂移。

---

## ★ 产出格式

### 你唯一要写的文件是一份 JSON。**你不写 HTML。**

```
你写一份 JSON  →  node scripts/build.mjs <JSON路径> "<片名>"  →  Output/资产盘点-<片名>.html
   (只有数据)         (它负责套模板 + 结构预检)                       (成品)
```

数据契约在 `templates/report-shell.html` 顶部注释里，照它写。字段就这些：

```
meta:         title / script / sceneCount / assetCount / variantCount / worldview（必填，
              年代/阶层/风格结论，见第 0 步；显示在页面顶部剧本路径正下方）
              + charCount / outfitCount / propCount（对应数组非空时必填）

assets[]      场景（必填，至少一份）
              tag(@loc_…) / name / scenes / note(可选) / brief / variants[]
              / basedOn(可选，指向同一物理空间内"主区域"场景的 tag，见第 2 步之二)
characters[]  角色底档（可选）—— 脸/年龄/身形/发型，写一遍，**不登记场次**
              tag(@char_…) / name / note(可选) / brief
outfits[]     服装资产（可选）＝ 出图单元「角色 + 一套服装」
              tag(@char_<角色>_<服装>) / char(指向底档) / name / scenes / note / brief / variants[]
props[]       关键道具（可选）—— 只收跨场景的
              tag(@prop_…) / name / scenes / note / brief / refs[](可选) / variants[]

variants[]（三类通用）: tag(以主资产 tag + "_" 开头) / name / scenes / brief
```

★ **三类同住一份 JSON，不是图省事**：`props[].refs` 要能被校验"你引用的那个 tag 确实存在"。
拆成三份数据源就拦不住「`@char_xiaoming_xuesheng` 写错一个字」，
而拼错的后果是下游凭文字另画一遍 —— 那正是"间接引用"最要避免的事。

★ **note / brief / worldview 都不许留会话引用。** 真事：写盘点时会顺手对着当次对话里
用户贴的截图判断（"这张图第 9 张显示……"），如果把这类引用原样留进 `note`
（比如"[Image #9]"这种插图编号、"如图所示"、"见上图"），使用者拿到产出时
那次对话早就翻篇了，这句话变成谁也读不懂的哑谜。**判断依据要转述成剧本里的
证据**（"S03 阿龙说'还是老地方'"），不要引用对话工具给截图编的号。
`build.mjs` 用 `PLACEHOLDER_RE` 挡最露骨的写法（方括号包住的非纯数字内容），
但词表挡不住"如图所示"这类不带括号的写法，动笔时自己留意。

★ **角色/道具是可选的。** 只做场景盘点仍然合法，空的那张表不出 Tab；
只剩一张表时整个 Tab 栏都不出。

### 禁止清单（违反任何一条，页面样式一定会坏）

- ❌ **不许用 Write / Edit 生成或修改任何 `.html` 文件**
- ❌ **不许在 JSON 里出现任何 HTML 或 CSS**（`build.mjs` 会扫，扫到就拒绝）
- ❌ **不许自己写 CSS、自己排版**
- ❌ **不许改 `templates/report-shell.html`**（静态外壳，一个字都不动）

### 怎么确认自己做对了

页面里有一条 `SELFTEST` 横条，**但它默认不显示给人看** ——
那是自检凭证，不是给读者看的内容。它始终在 DOM 里，用 `--dump-dom` 抓：

```
SELFTEST  loc=19+5v  char=4/12+3v  prop=6+2v  rows=37
          brief=N/N OK  var-brief=M/M OK  lane=N/N OK  no-backstory=N/N OK
          char-lane=N/N OK  anchor=N/N OK  scenes=N/N OK  refs=K/K OK
          coverage=77/77 OK  xcover=OK  font=OK
```

**这条必须存在且全项 OK**（怎么抓见第 8 步）。抓不到它 = 这页不是 `build.mjs` 生成的，
**推翻重做，不要试图手动修 HTML**。
调试时想用肉眼看它，在网址后加 `?selftest=1`。

badge 与 build.mjs 的分工是有意的：
- **"这一份产出有没有缺东西"**（brief / var-brief / lane / no-backstory / char-lane /
  anchor / scenes / refs / coverage / xcover）两边都有 —— badge 让一眼看出够不够格
- **账目结构类**（标签格式 / 重复标签 / 计数自洽 / char 指向 / 跨场景门槛）只在 build.mjs
  —— 不合格根本生不出页面

★ `anchor` 只查**有没有写参照物**（只查场景，服装已改成推荐不强制）。**位置对不对闸门查不了** ——
那是三步推导和第 9 步汇报的事。

---

## ARGUMENTS 三级降级

- 显式给了剧本路径 → 用它
- 没给 → 从当前对话里找最近提到的剧本文件或场景讨论
- 都找不到 → **问用户**。绝不瞎猜路径，更不要拿一个不存在的剧本编内容

## 上岗第一步：读底档

skill 根目录 = **本 SKILL.md 所在的那个目录**。`scripts/` 下的脚本用 `import.meta.url`
定位自身，**从哪个 cwd 调都行**。

- `references/how-to-infer-worldbuilding.md` —— **调性篇（比场景篇更先读）**：
  动笔切场景之前，先定年代 / 阶层 / 风格三件事，否则 brief 会凭直觉套错调性
- `references/how-to-write.md` —— **场景篇 + 三类共用纪律**（切场景 / 去重 /
  brief 怎么写 / 变体怎么定 / 考据的分寸）。动笔前必读，这是本 skill 最要紧的一份
- `references/how-to-write-character.md` —— **角色篇**（三层压两层 / 收录门槛 /
  底档与服装各写什么 / 换装不是变体 / 表情性格不写 / 颜色能不能写）
- `references/how-to-write-prop.md` —— **道具篇**（跨场景门槛 / 状态变体 /
  ★ `refs` 间接引用与参考生图）
- `references/handoff-boundaries.md` —— 谁管哪一格 + 三类各自的锚点 + 一致性怎么保
- `knowledge/scene-archetypes.md` —— **场景调性**速查（身份定形制、旧≠没人管、时代错误清单）
- `knowledge/costume-archetypes.md` —— **服装调性**速查（身份定服制、别抄服制术语、
  旧≠没人管的衣服版）

两份 archetypes **都不是尺寸表**，是"帮你想清楚该有多讲究"的判断表，落笔要翻成大白话。

---

## 工作流程

**顺序有讲究：先定调性，再场景，再角色，最后道具。** 因为道具的 `refs` 要指向已经建好的
服装资产或场景资产，而交叉对账（角色/道具的场次必须在场景表里出现过）也要场景先立住；
而场景/角色/道具的 brief 调性对不对，则要靠**先定好年代/阶层/风格**才判断得出来
（见下面第 0 步——这一步在 brief 写作时是心里有数、不是另出一份文档）。

### 第 0 步：★★ 先定调性——年代 / 阶层 / 风格（详见 `references/how-to-infer-worldbuilding.md`）

这一步是 EV 在实跑《北王刀》时揪出来的：没有先定这三件事，brief 只能凭"苏家""世家"
这类词的第一印象去写，而直觉常常是错的。真事：《北王刀》的苏家客厅被我写成了
"整体装修传统偏旧，木质家具居多"——听起来像个老式中式老宅，但这个故事的物质文明
其实是当下（剧本里出现的是伦敦苏富比拍卖行、世界银行黑卡、兰博基尼 Aventador、
写字楼、智能手机——全是真实世界当代的品牌/机构），苏家又只是个"几乎要跌出三流"
的没落世家，苏怀瑾这一房自己住的还是被罚到只剩死工资的那一支，房子该是**当代都市
高端小区里的跃层公寓**，不是哪个年代的中式老宅。措辞对不对全靠这三件事定，
定错了，后面写十份 brief 错十份。

**三件事分别怎么定，简述（完整方法与更多反例见 `references/how-to-infer-worldbuilding.md`）：**

1. **年代** —— 找剧本里出现的**真实世界品牌/机构/科技**（拍卖行、银行、车型、
   通讯方式）当锚点，比架空称谓（国名、官职名）更可靠。很多短剧是"现实物质
   生活 + 架空政治军体系"两条线并存，光看到"天策上将军""兵部"这类词不能
   断定是古代剧或架空朝代剧——要看物质生活层是不是也跟着架空了。
2. **阶层** —— 不要被自称的身份词（"世家""老太君""大人"）牵着走，要抓
   **具体数字与比较**：总资产、跟故事里其他角色的量级差、家族内部不同分支
   的贫富落差、外部人物怎么评价这家人的地位。同一句"很有钱"，在故事内部的
   坐标系里可能只是垫底。
3. **风格** —— "现代物质生活"与"架空政治军事体系"是两条平行线索，同一个人
   在不同场景可能切换调性（穿西装 vs 穿蟒袍），要按每个具体场景判断该用哪条，
   不能笼统套一个风格到全剧。

★ **同一个家族名下可能有多处地产，规格不同**——不要看到名字相近的场次
（"苏家客厅" vs "苏怀瑾家客厅"）就想当然合并或拆开，要靠**在场人物 + 空间
限定词**（院子/客厅/别墅/小院）交叉验证，这是去重环节最容易连带出错的地方。

**不产出单独的世界观文档，但结论要写进 `meta.worldview`。** 判断结果既要体现在每句
brief 的调性里，也要在 `meta.worldview` 留一两句话的结论（`build.mjs` 会拒绝空着的）——
这一步的意义太大，只留在聊天记录里等于没有记录：页面顶部剧本路径正下方专门有一块
高亮条显示它，使用者点开页面第一眼就看得到，不用翻聊天记录找这段判断。
`meta.worldview` 只写结论本身（几句话），别把依据也塞进去——吃不准的判断依据，
跟去重、锚点一样，写进对应资产的 `note` 里，让人能复核（依据里引用剧本证据，
不要引用"上面那张截图"这类当次对话才懂的说法）。
第 9 步汇报时同样要点出这条判断，跟页面上这句保持一致。

### 第 1 步：切分场景

剧本有 `INT./EXT.` 标准场记格式 → 照切。没有（短剧大概率没有）→ 按**地点 + 时间**切。
场次号沿用剧本自己的编号（`11-1` 这种），别自己另编一套 —— 使用者要拿它对回剧本。

### 第 2 步：按地点去重成场景资产（**这一步是整份盘点的价值所在**）

**同一地点在剧本里反复出现，只建一份资产**，把所有场次登记进 `scenes`。

**去重要靠台词证据，不能靠地名。** 剧本里同一处会有好几个叫法，不同处也会共用一个名字。
四种真实情况（判法见 `how-to-write.md`）：一名多地 / 多名一地 / 地名指错 / 一院多空间。
拿不准就在 `note` 里如实登记依据与存疑，**不要静悄悄合并**。

命名 `@loc_<slug>`，小写英数下划线，全项目一套字典。

### 第 2 步之二：连续场景 —— 同一物理空间内的衍生区域

**连续场景**指同一个物理空间里的不同区域：同一套公寓的客厅、门厅、卧室，
分别是各自独立的场景资产（切不切开的判据仍然是"能不能同时入镜"，
见 `how-to-write.md`），但它们生图时不该各画各的——后面几个区域应该拿
**这组里最先出图的那一份**当参考，用图生图模式续风格、续色调、续建材质感，
不是每个房间都凭空文生图各画一版、风格互相对不上。

判据是剧本证据，不是"看着像"：同一处地产/同一栋建筑的不同部分（人物从一个
区域走进另一个区域、同一处的门牌楼层号对得上）才建立这层关系；纯粹风格接近
但剧本里是两处不相干的地方，不算。

登记方法：选这一组里**最先建、最具代表性**的那份资产当**主区域**
（通常是使用者会先看到、后面房间都从它派生的那间，比如客厅），
其余区域在自己的资产上填 `basedOn`，指向主区域的 tag：

```json
"basedOn": "@loc_gongyu_keting"
```

页面会在填了 `basedOn` 的那一行打一个绿框「◈ 风格参考」，提示这一份要拿
`basedOn` 指向的那张成图做参考生图，不是纯文生图。`basedOn` 只能指向
**另一份场景的主 tag**，不能指变体，也不能指自己或绕成一个环——`build.mjs` 会拦。

★ **同一组里的场景，在 `assets[]` 数组里的顺序必须排在一起**，中间别插别的
场景——使用者要在表格里一眼看出"这几行是同一个家"，隔太远会被当成不相干的
场景。这一条 `build.mjs` 也会检查（下标必须连续）。

不是所有场景都要建这层关系——只有剧本里同一处地产/建筑确实有多个区域入镜时
才需要，孤立场景（这个剧本只这一处的仓库、公园）不用填 `basedOn`。

### 第 3 步：逐场景写 brief

**brief —— 几句话，300 字以内。★ 只写这个场景的需求，不写为什么有这个需求。**

写这四样，全部是**画面里看得见的东西**：

1. **这是什么地方** —— 什么类型的空间、露天还是室内、怎么围合的、白天还是夜里
2. **形制的具体样子，但要用通俗说法** —— 不是"讲究/寒素"这种评语，也不是
   "门屋一间""不用斗栱藻井"这类术语（图像模型没见过，写了跟写尺寸一样是噪声），
   是"门面窄小、梁柱柱头没有精雕装饰；梁柱是沉香檀木，堂内地面铺釉砖"。
   查 `scene-archetypes.md` 的形制表定档次，但**写出来的必须是通俗对比，不是档次结论也不是术语**
   **每一格必须脱离别的格单独读懂** —— 把它单独拿给一个没看过这张表的人，
   他得知道这是哪儿。指本场景内部的（"旁边一片空场"）不算跨行。
3. **一两处最有说服力的痕迹** —— 踩白的光带、绳槽、跪凹、碱霜、辙痕
4. **参照物一件** —— 抗漂移的锚点。★ **它不是"这地方最好看的一件东西"，
   而是给下游一套相对位置的说法**（`tie the staging to it`），所以**必须落在演员真正
   表演的那个位置上**。推导顺序不能反：

   ```
   第 1 步  这场戏在哪儿演？只看剧本的 Δ 动作行，读出表演焦点落在哪一点
   第 2 步  那一点上，这类空间本来会有什么不可移动的东西？
           ★ 优先用**已经写进 brief 的环境要素**，不要另外发明一件
   第 3 步  过五条：不可移动 / 唯一 / 好认 / 画面里看得见 /
           位置得是这类东西本来就在的位置
   ```

   **反过来做（先想一件好东西、再给它安位置）= 必然编造。** 踩过：给露天庭院写
   「廊柱下一对石雕瑞兽」—— 石狮长在大门两旁或柱头**之上**，廊柱柱脚只有柱础；
   而那句 brief 其实**已经写了正确的锚点**（"石阶被踩得温润发亮"），
   剧本里跪拜、接旨、被刺、倒地全发生在正厅台基前的踏道跟前。
   **能用痕迹就别用有规制的器物** —— 知识缺口在没有来源约束时必然变成编造。
   依据写进 `note`，跟去重一样要能被复核。完整论述见 `how-to-write.md` 三之三，
   位置避雷清单见 `scene-archetypes.md` 三之二。

**不写（这一条最容易复发）**：

| 不写 | 为什么 |
|---|---|
| 剧情与人物（"沈云卿在这儿被罚跪"） | **场景资产图里不该有人**，人在角色表里；某个镜头谁在场是下游的事 |
| 场次统计（"只用一场"、"全片戏最重，十五场"） | 账目在第一列已经有了，重复；而且总会牵出剧情 |
| 场次号（`38-2`、`11-1 到 15-1`） | 同上。`scenes` 字段就是干这个的 |
| 跨行指代（"**同一处**府第正厅之内"） | 指向另一行，**那行的内容这格里没有** |
| 跨场景比较（"全片最讲究的一处"） | **离开上下文读不懂**，图像模型没有那个上下文 |
| 因果解释（"有仆役日日打理，所以干净"） | 直接写结果（"阶面无一片落叶"）。过程画不出来 |
| 尺寸、材分、材料清单、道具表 | 图像模型渲不出来，纯占注意力预算 |
| 机位、景别、光比、**配色方案** | Lira 与 color-script 的活。**固有色可以写**（见下） |

★ **固有色归我们，配色方案归下游。** "褪成灰蓝的搪瓷门牌""屋顶大片乌黑发亮"照写；
"整体偏冷""低饱和""蓝占七成"不许写。理由见 `handoff-boundaries.md` 第二之二节。

反例（真发生过，被 EV 抓出来）：

> ✗ 只用一场夜戏。全片最讲究那处民宅的正堂：五间进深九架，**勋戚人家该有的规格都在**。
> ✓ 皇室远亲府第正厅之内，室内，夜里。厅堂开阔，**梁栿粗壮、其上施着彩绘**。
>   板门闭合，庭院隔在外面，只点厅内的烛。长案两端那对落地烛台是参照物。

`build.mjs` 拦：31 个下游词、场次号、"……时用"这类用途说明、通用人物词。
**人名穷举不了，闸门只是兜底 —— 动笔前就别写。**

### 第 4 步：定场景变体（**默认没有变体**）

> **衍生变体 = 同一份资产，变化之后。** 场景可变三样：
> **机位**（切到反打）·**氛围**（变成雨天/秋天）·**状态**（变成废墟）。
> 它必须**源自同一个空间** —— 主场景的墙、地、参照物还在那儿。

**判据：主场景那个空间，在这个变体里还在画面里吗？** 不在就不是变体，
是另一个场景，该另建一份资产。踩过：给露天庭院挂了个「改成夜里，视点移进正厅
明间之内，庭院不入画」的变体 —— 镜头进到另一个房间了，原空间一个像素都不剩。
代价是双份的：图生图没有共同像素必然失败，而且**盘点上漏掉了"正厅内"这个真实场景**。
`build.mjs` 拦最露骨的写法，但词表抓不全，每写完一个变体自己回头问一句。

★ **机位变体不是默认项，判据是这场戏是不是真的有反打——但"反打"不等于"内外"：**

| 这场戏是 | 机位变体 |
|---|---|
| 过渡戏 / 空镜 / 全程只有一个机位在拍（推门走过、环境交代） | **没有** —— 只出主视角一版 |
| 有不止一个观察点在用 | 除主视角外，**加一份反打机位的变体** |

"有不止一个观察点在用"的常见形态，**不要只认第一种**：

- 人物隔着门/窗内外来回对话（这是最直观的一种，但不是唯一一种）
- **同一个房间内，两端各有一处调度焦点，剧本让人物在两端之间来回、或分居两端对峙**
  —— 甬道尽头与台基前来回切、审判台与阶下犯人来回切，都属于这一种，**跟"内外"无关**
- ★ **仪式性/礼仪性的大场面空间**（婚宴/寿宴/签约酒会、公堂、灵堂、法庭这类有
  "入口 ↔ 主位/主桌/舞台/香案"轴线的场景）——这类空间**几乎总有不止一个机位在用**
  （人物从入口走向主位、主位一方回望入口），**默认按"很可能需要核实反打"去查，
  不要因为它是"同一个房间、不是内外"就直接判定没有变体**

判断依据是剧本的 Δ 动作行有没有写出明确的两端调度或对峙——不是"这场戏换个机位
应该好看"，好看不是标准，剧本真的要拍两个方向才是标准。★ **一份场景资产如果
覆盖了几十场戏、服务不止一桩剧情事件**（同一处宴会厅先后办了寿宴又办了签约宴），
**把这几十场的 Δ 动作行都过一遍再下判断，不要只看第一场戏的调度就定论** ——
不同事件在同一个空间里的调度经常不一样，后面的场次才可能是反打真正的证据来源。

★ **机位变体的 brief 只写切到哪个方向，不用描述反打之后看到什么**：
`build.mjs` 里那句"切到自正堂台基上向南反打的视角"已经是范本 —— 只说方向，
不接着写反打那侧有什么家具、什么陈设。那一侧长什么样交给图像模型的先验去补，
它比我们会画。**唯一的例外**：反打那个方向上有一件必须露出的关键锚点或跨场景
复用的道具（比如反打正对着的墙上就挂着后面要用参考生图的那幅画），这种情况才
补一句点出那件东西，其余什么都不追加。

**只写剧本真的要拍的**，不凑齐五视图、不穷举组合。一句话说清改什么：

| 轴 | 一句话 |
|---|---|
| 机位 | 切到自正堂台基上向南反打的视角 |
| 氛围·时间 | 改成夜里，两侧灯杆全部点起 |
| 氛围·天气 | 改成雨天，地面积水 |
| 氛围·季节 | 改成秋天，满地枯叶，枝头只剩零星黄叶 |
| 状态 | 改成大火烧过之后，东半院覆一层灰烬 |

变体标签必须以主资产的 tag 加下划线开头（`@loc_shenfu_charcoal`）。

**变体 brief 只写这一版长什么样**，同样不许出现人物、场次号、用途说明。
踩过：`切到楼下街面向上仰视挑台的视角，3-1 沈云卿下楼走到人群里时用` ——
一句话里人物、场次、用途三样全犯。正确的是前半句，句号。
**变体名也一样**（「炭火刑场」的"刑场"、「夜·议事」的"议事"都是戏，不是场景 ——
改成「炭火带」「夜」）。

### 第 5 步：盘角色（详见 `how-to-write-character.md`）

**三层压两层。** 角色天然是「人 → 几套服装 → 每套的几个状态」，落到数据里：

| 层 | 落到哪 | 为什么 |
|---|---|---|
| 人（脸/年龄/身形/发型） | `characters[]` **底档** | 跨服装不变的那部分，写一遍。**不登记场次** |
| 人 + 一套服装 | `outfits[]` **资产** | **出图单元** —— 光有一张脸出不了图 |
| 那套衣服坏了/脏了 | `outfits[].variants[]` | 状态变体 |

**收录门槛（与道具同一条尺子）：跨 ≥2 场，或剧本给了名字。**
一次性配角（`3-1` 的卖菜大娘、围观群众）不建档 —— 没有漂移风险，交给图像模型的先验。
但**要在汇报里点一句「另有 N 个一次性配角未建档」**，账目要诚实。

**底档写四样**：年龄段与性别 / 脸（含一处独有辨识点，如左眉尾一道旧疤）/ 头发 / 身形。
不写五官比例 —— 那跟写"墙下隔减广约 1.09m"是同一个错。

**服装 brief 从第一个字起就是衣服**（脸在底档里，别重复）：形制 / 料子与做工（通俗说法，
不抄"襦裙""褙子"这类服制术语，查 `costume-archetypes.md`）/ 一两处使用痕迹 /
**（推荐，不强制）参照物一件 —— 不摘的那件配饰**（腰上那条帆布工具带、左手腕那块方形钢壳表）。
`build.mjs` 已不再要求服装 brief 里出现"参照物"三个字 —— 使用痕迹本来就够跨镜头识别，
再强制一句容易逼出复述式的废句；想写更好认就按下面方法推导，不写不算漏项。

★ **服装的参照物走与场景相同的三步推导**（先读这几场的 Δ 动作行看人在干什么，
再挑那个姿态下**看得见**的那件不摘的配饰），并且多一条陷阱：**会被姿态挡住。**
「腰侧那枚素银小铃」在站姿里很好，但这套衣服的戏全是跪着接旨、腰侧被裙摆挡死，
它就不是这套的好锚点。踩过的另一个：「衣领**内侧**缝着的名字布牌」—— 任何姿态都不入画。
**同样优先用痕迹**（磨痕、绳印、镣铐痕、普通布带全稳）而不是有规制的器物
（扳指、翡翠镯、方形胸章、"家纹"全是雷）。

★ **换整套衣服不是变体，是另一份服装资产。** 判据换个词：
**这套衣服，在这个变体里还穿在身上吗？**
撕破 / 湿透 / 蒙尘 / 染血 → 有共同像素，是变体。
换一整套 → 脸还在但衣服一个像素都不剩 → 另建资产（新 tag，`char` 指同一个底档）。
`build.mjs` 拦 `另一套 / 另一身 / 换一套 / 换一身 / 整套换 / 换成另 / 改成另一`。

★ **角色专有三条不写**：**表情情绪眼神神情**、**姿势动作手势**、**性格评语**
（温柔/高冷/精明/气质/看起来很…）。
角色资产图是**中性站姿的形象底板**，不是一个镜头 —— 表情姿势是 Lira 与 CINEDANCE
每个镜头各自的活；性格渲不出来，跟写"讲究/寒素"是同一类错误。
要表达"这人不好惹"，写看得见的东西（下颌线硬、衣领扣到最上一颗）。

★ 场景那条 `PEOPLE` 闸门（人物/角色/众人…）**不管角色表** ——
角色资产图里当然要有人。那是刻意按类型分派的。

### 第 6 步：盘关键道具（详见 `how-to-write-prop.md`）

**★ 只收跨场景的。** 判据一句话：**这件东西下一次出现时，观众会不会认出它是同一件？**

理由与「一个中式庭院」同源：**只有跨场景复用的东西才有漂移风险，才值得建一份资产锁。**
只出现一次的道具交给图像模型的先验就行 —— 列出来反而是在压先验，
跟给场景写 11 件带位置的道具表是同一个错。`build.mjs` 会拒绝只覆盖 1 个场次的道具。

**brief 写四样**：这是什么东西 / 料子与做工（通俗说法）/
**一两处最好认的细节 —— 这就是道具的参照物** / 有没有间接引用。

★ **道具是三类里唯一不走三步推导的。** 场景和服装的锚点是**位置参照**，必须从表演位置推出来；
道具跟着人走、没有固定位置，它的锚点是**物件自身的一处特征**。所以只问"这件东西身上
哪一处最好认"，`build.mjs` 也**不要求道具 brief 里出现"参照物"三个字**（服装现在也一样，
只有场景是硬性要求）。
共通的两条仍在：那处细节必须**看得见**，且**优先用痕迹损耗**而不是有规制的纹样形制。

**与场景的分界是能不能搬得动**：搬不动的（地秤、落地烛台、石雕瑞兽）是**场景的参照物**，
写在场景 brief 里，不在道具表里再开一份账。

★ **状态变体：同一件东西坏了。** 碎裂/烧焦/褪色/沾血 → 变体。
**仿制品/赝品/替代品 → 另一件物，另建资产**（`@prop_yupei_fake`）——
真假调换是最常见的桥段，混成一份等于盘点漏了一件道具。
`build.mjs` 在变体 brief 里拦这几个词（主资产本身就是赝品是合法的）。

### ★★ 第 6 步之二：间接引用（`refs`）—— 最容易被漏掉的一件事

有些道具的**画面上包含另一份资产的形象**：照片、合影、油画、肖像、绣像、
建筑画、舆图、通缉令。

**这类道具不能用文生图。** 凭文字再描述一遍那张脸，出来必然不是同一个人。
它必须**走参考生图**：先出被引用那份资产的成图，再以那张图为参考生成这件道具。

在 `refs` 里登记引用了谁，页面会给它打一个蓝框「⚑ 必须走参考生图」：

```json
"refs": ["@char_along_gongzhuang", "@loc_warehouse"]
```

**★ refs 必须指到服装级或场景级，不能指到角色底档。**
底档只有一张脸没有衣服 —— 照片上的这个人穿的是哪套？下游不知道该拿哪张参考图。
`build.mjs` 会拦下指到底档的写法，并把该角色现有的服装 tag 列给你挑。
引用场景同理，要指到具体资产或**具体变体**（白天那版还是烧过之后那版）。

**refs 带来生成顺序** —— 有 refs 的道具排在被引用资产之后出图，汇报时把顺序说出来。

**什么不算间接引用**：道具装在另一件道具里（那是两份独立资产，`build.mjs` 会拦
refs 指向道具）；道具出现在某个场景里（那是 `scenes` 字段的事，不是画面上画着）。

### 第 7 步：写 JSON → 生成页面

```bash
node scripts/build.mjs <数据JSON路径> "<片名>"
# → Output/资产盘点-<片名>.html
```

不合格直接拒绝生成，并列出出错路径（`outfits[3].brief`、`props[1].refs[0]` 这种）和改法。

★ **有一条对账只有三类同住一份数据时才做得到**：
**角色/道具登记的场次，必须都在场景表里出现过。**
有一场戏有人有道具却没有地方，只有两种可能 —— 场景表漏了一场（那场到时候没有场景可用，
这是盘点最严重的失败），或者场次号写错了。badge 里叫 `xcover`。

### 第 8 步：自检截图（**不许生成完就交差**）

★ **截图必须带 `?tab=all`。** 页面是三个 Tab（场景 / 角色 · 服装 / 关键道具），
默认只显示第一张 —— 不加这个参数就只截得到场景表，另外两张等于没验。
`?tab=all` 是「全部展开」模式，三张表同时铺开。

```bash
B="$LOCALAPPDATA/Google/Chrome/Application/chrome.exe"
[ -f "$B" ] || B="/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe"
T="$(cygpath -m "${TEMP:-/tmp}")"
# ★ P 不要手拼 —— build.mjs 最后一行会打印现成的 P="…"，直接 copy
P="<粘 build.mjs 打印的那行>"

# ① 看一眼：badge + 三张表的排版（?tab=all 缺了就只截得到第一张表）
"$B" --headless --disable-gpu --screenshot="$T/sb-1.png" \
  --window-size=1600,2600 --virtual-time-budget=9000 "$P?tab=all"

# ② 验全量数据都渲得出来（截图只能看到首屏，末行要靠 DOM 验）
#    DOM 里三张表始终都在（隐藏的 Tab 只是 display:none），所以这一步不用带参数
"$B" --headless --disable-gpu --dump-dom --virtual-time-budget=9000 "$P" > "$T/dom.html"
grep -o 'SELFTEST</b>[^!]\{0,700\}' "$T/dom.html" | head -1   # badge 全项
grep -c 'id="badge" class="fail"' "$T/dom.html"                # 必须是 0
grep -c 'id="r<末行序号>"' "$T/dom.html"                        # 必须是 1

# ③ 验 Tab 切换与深链（默认 loc / 指定 prop / 全展开 / ?row 自动切到那一行所在的表）
for u in "" "?tab=prop" "?tab=all" "?row=<角色表里某一行的序号>"; do
  "$B" --headless --disable-gpu --dump-dom --virtual-time-budget=9000 "$P$u" \
    | grep -o 'class="sect on" id="sect-[a-z]*"\|id="sect-[a-z]*" class="sect on"'
done
```

**末行序号 = 场景数 + 服装资产数 + 道具数**（`rows=` 那个数；底档的分组行不计）。

Read `sb-1.png`。**第一件事：确认 SELFTEST 横条存在且全项 OK。**

然后肉眼确认：中文不乱码、三张表都在、表格没横向溢出、蓝框的参考生图提示在该在的行上。

> 注：`?row=N` 会自动切到那一行所在的那张表、高亮并滚过去，但**滚动只在真人浏览器里有用**
> —— 无头截图不跟随滚动。它可以用来验 Tab 自动切换（看哪个 `sect` 带 `on`），
> 但别拿它做排版截图。

### 第 9 步：汇报

- ★ **世界观关键判断：年代 / 阶层 / 风格的结论和依据**（结论已写进 `meta.worldview`、
  页面顶部就看得到；这里把依据也讲清楚——这条决定了后面所有 brief 的调性对不对）
- **原始场次数 / 去重后场景数 / 场景变体数**
- **角色底档数 / 服装资产数 / 状态变体数**；**关键道具数 / 状态变体数**
- **哪些场次被并到了一起、依据是什么**（这是最要看的一项）
- **哪些去重存疑、需要用户拍板**
- ★ **哪些参照物是推断的、位置拿不准**（锚点的错误无法由单张图发现，症状是"两张图对不齐"
  —— 要出两张并排比才看出来。所以它必须在汇报里点名，不能等出图之后才暴露）
- **哪些配角与道具你判断为一次性、没有建档**（账目诚实比表长好看重要）
- **有 refs 的道具，以及它带来的生成顺序**（被引用的图要先出）
- 哪些变体是"预判会用到但剧本未明写"，请用户确认
- 页面路径 + 自检结果

**交付到此为止，停下等审阅。**

---

## 边界

- **不决定摄影机位 / 景别 / 镜头语言 / 光质 / tech block。** Lira 的活，它有踩过坑的公式。
- **不写配色方案**（色调、冷暖、比例、饱和、grade）。**固有色是我们的** ——
  "藏青夹克""褪成灰蓝的门牌"照写，那是识别特征不是调色决策。
- **不写表情、情绪、姿势、动作。** 资产图是形象底板，不是镜头。Lira 与 CINEDANCE 的活。
- **不写材质纹理、叶片形状、苔痕落点、雾气氛围。** 图像模型的先验比我们会画。
- **不做戏剧结构判断**（`higgsfield-scene-engine` 的事）。假设场景已值得拍。
- **不生成图像，不调用 Lira，不改剧本。**
- **变体不穷举组合。** 没被剧本用到、也没人提起要预留的状态，不主动发明。
- **一次性配角与一次性道具不建档**，但要在汇报里如实点出来。
- **不做深度考据。** 网剧短剧不需要 —— 年代与国别定个大概、不出戏就行
  （第 0 步"先定调性"做的就是这个"大概"，靠的是剧本里现成的真实品牌/机构/数字当锚点，
  不是另外去查资料考据；查资料考据的，是下一条讲的"参照物选对"这一件事）。
  省下来的力气花在**去重判对**、**参照物选对**、**refs 指对**这三件真正影响成片的事上。
  ★ **但"参照物选对"里含一条硬约束**：器物的**位置**必须是它本来就在的位置
  （`scene-archetypes.md` 三之二有避雷清单）。这不是考据 —— 判据是
  "图像模型的先验会不会把它放在这儿"，跟强先验争位置比用生僻词输得更彻底。
  拿不准就换一件痕迹类的，别去补考据。
- **不写尺寸。** 这条最容易复发 —— 尺寸看起来"更专业"，但图像模型渲不出来，纯占注意力预算。

---

## 开发/维护备注

- **校验常量在两处各存一份**（`templates/report-shell.html` 与 `scripts/build.mjs` 里的
  `DOWNSTREAM` / `PLACEHOLDER_RE` / `EMOTION` / `TRAIT` / `OUTFIT_SWAP` / `PROP_SWAP` 等）。
  自包含 HTML 没法 import，只能重复；**改一处必须同步另一处**：
  ```bash
  node scripts/check-sync.mjs   # 自动发现 build.mjs 里的常量 + 逐项比对 + 检查数据契约注释
  ```
  它抓过四次真漏：① 新增必填字段时校验两边都改了、外壳顶部的数据契约注释没跟上；
  ② 新增 `USE_TRACE` 时它只进了 build.mjs，**而当时 check-sync 用的是硬编码常量名列表、
  里面没有它，检查器自己也瞎了** → 改成自动发现；
  ③ 契约检查在 `BASE_FIELDS` 被删后变成**空转的假绿**（`grabArray` 返回 null，
  `missing` 恒为空）→ 改成硬编码 `REQUIRED` 并在注释里点名这个陷阱；
  ④ `REQUIRED` 里裸写的短字段名会被别的字段名**包含**掉（`char` 是 `charCount` 和
  `characters` 的子串，裸写等于永真）→ 改成带引号写 `"char"`。
- **tag 格式正则刻意内联、不提成 `ALL_CAPS_RE` 常量。** check-sync 会要求外壳有一份同名常量，
  而标签格式属于"账目结构类"，按分工只该在 build.mjs 拦。别好心把它提出去。
- **`{{DATA_JSON}}` 在外壳里只能出现一次。** `String.replace` 只换第一处 ——
  注释里再写一遍会把数据注进注释、真正的槽位反而空着（页面全白）。
  外壳顶部的数据契约注释因此刻意不写这个字面量，`build.mjs` 也在注入后断言无残留。
- **词表穷举不了。** 它是提醒不是护栏 —— 没报错不代表描述干净，写完自己回读一遍。
  撞上合法术语被拦，往豁免逻辑加，**不要去改正确的内容**。
- ★ **「被声明为必须、却没有闸门」的规则是这个 skill 最危险的一类漏洞。**
  参照物就是这么漏的：SKILL 里一直写着"参照物那一句别省"，却是全 skill 唯一
  一条没有任何检查的必须项 —— 于是既没有强制、也没有来源约束，锚点成了唯一
  可以凭空编造的字段（"廊柱下一对石雕瑞兽"就是这么来的）。
  **维护时的自查：SKILL 里每一句"必须/别省/一定要"，`build.mjs` 里有对应的检查吗？没有就是下一个坑。**
  顺带记下另一半原因：**锚点的错误是全表唯一无法由单张图发现的**
  （症状是"两张图对不齐"），错误反馈最慢，所以最容易积累 —— 这类字段值得优先加闸门。
- ★ **闸门也会走偏方向、需要回撤 —— 服装那半的参照物闸门就是一次。** 补齐闸门半年后，
  EV 看实际产出发现服装 brief 里经常出现「那圈洗得发白的袖口是认得出的参照物」这种句子 ——
  为了让闸门认出"参照物"三个字，把点 3 已经写过的使用痕迹又复述一遍，纯粹是凑闸门的废话。
  服装的使用痕迹本来就承担了跨镜头识别的作用，不像场景那样"没有位置参照就没法定站位"，
  于是把这半条闸门撤掉，改成推荐不强制（`checkAnchor` 只留场景一处调用）。
  **教训：闸门防的是"漏写"，但如果必写的东西已经被别的必写项覆盖了，闸门只会逼出复述。**
  加闸门前先问一句"这条要求是不是已经被另一条覆盖了"，别的必写项能顶上就别重复加锁。
- `scripts/make-test.mjs` 是闸门验收工具，不是运行时的一部分。改了预检逻辑后跑一次
  —— **抓不住的闸门等于没有**。
  它现在有 36 条违规用例。★ **分清两种闸门**：
  - **字段闸门**（"这个字段必须写、必须写到多细"）—— 会把描述逼成规格书，**禁止扩张**。
    曾经有过 30 条这种，就是那次走偏的产物
  - **类型分派与交叉引用完整性**（PEOPLE 只管场景、refs 指到服装级、
    角色场次必须在场景表里）—— 一类资产变三类，这类检查必然变三倍，是该有的

  加角色道具时新增的 11 条全是后者，补参照物闸门时又加了 2 条（也是后者：
  它守的是"必须项有没有闸门"这个结构缺口）。往下再加之前，先分清自己要加的是哪一种。
